<h1>New Bookmark</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>

  <div class="actions">
    <%= f.submit "Create Bookmark" %>
  </div>
<% end %>
